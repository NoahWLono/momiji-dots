# Systemd-based initramfs with an early LUKS2 password prompt.
# This drop-in overrides HOOKS from /etc/mkinitcpio.conf.
HOOKS=(base systemd autodetect microcode modconf kms keyboard sd-vconsole block sd-encrypt filesystems fsck)
